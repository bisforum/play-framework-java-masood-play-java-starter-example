# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET         /statistics          controllers.AsyncController.getOrderStatistics
POST        /add                 controllers.AsyncController.addOrder()
